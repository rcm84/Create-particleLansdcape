# Assets

This folder is reserved for starter-kit owned media.

## Conventions

- `assets/heightmaps/` for reusable grayscale terrain sources
- `assets/textures/` for gradients, masks, and optional supporting textures
- `assets/screenshots/` for product shots, docs images, and release captures

Use `public/` for files that must be served directly by Vite demos.
